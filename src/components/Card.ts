import styled from "styled-components";

const Card = styled.div`
    background-color: #FFF;
    padding: 20px;
    border-radius: 20px;
    border: 1px solid #f1ecec;

    &:hover {
        border: 1px solid #4dc1cf;
    }

`;

export default Card;
